# Shared Config

Placeholder for shared configuration utilities (e.g. env loading, feature flags) used across apps and services. Currently each app loads its own config; this package can be extended when common config logic is needed.
