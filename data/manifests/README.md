## Manifests

This directory stores source manifests and provenance metadata for fetched datasets.

Manifests must not contain secrets. Large raw datasets must not be committed unless the license explicitly permits it and the team has confirmed.

