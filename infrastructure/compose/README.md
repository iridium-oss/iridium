## Compose structure

The primary local stack entrypoint is the root `docker-compose.yml` for developer convenience.

This directory is reserved for future compose fragments or environment overlays if the stack grows. If fragments are introduced, keep them small and document how they are combined.

